import React, { Component } from "react";
class MetaMaskNotConnected extends Component {
  state = {};
  render() {
    return <div>Meta mask not connected</div>;
  }
}

export default MetaMaskNotConnected;
